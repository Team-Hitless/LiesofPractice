<Window x:Class="LiesOfPractice.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:local="clr-namespace:LiesOfPractice"
        xmlns:vm="clr-namespace:LiesOfPractice.Viewmodels"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        mc:Ignorable="d" Title="Lies of Practice" 
        Height="650" Width="800" MinHeight="350" MinWidth="300"
        WindowStartupLocation="CenterScreen"
        ui:ThemeManager.IsThemeAware="True"
        ui:WindowHelper.UseModernWindowStyle="True"
        Topmost="{Binding AppSettings.StaysOnTop}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="*"/>    
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>  
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Row="0" Grid.ColumnSpan="2" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                    <Ellipse Width="10" Height="10" Margin="0,0,5,0"
                         VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="Green"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding AppSettings.Gamepath}" Value="">
                                        <Setter Property="Fill" Value="Red"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Padding="0" Height="17">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="Game found"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding AppSettings.Gamepath}" Value="">
                                        <Setter Property="Text" Value="Game not found"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                    <Ellipse Width="10" Height="10" Margin="0,0,5,0"
                         VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="Green"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsAttached}" Value="false">
                                        <Setter Property="Fill" Value="Red"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Padding="0" Height="17">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="Attached"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsAttached}" Value="false">
                                        <Setter Property="Text" Value="Not Attached"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                <Button Margin="5,0,0,0" Content="Launchgame" Command="{Binding LaunchGameCommand}"/>
                <Button Margin="5,0,0,0" Content="Select Gamepath" Command="{Binding SelectGamepathCommand}"/>
            </StackPanel>
        </Grid>
        <ListView ItemsSource="{Binding Pages}" SelectedItem="{Binding SelectedPage, Mode=TwoWay}"
                  Grid.Column="0" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" Padding="5,6"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <ui:ScrollViewerEx Grid.Column="1" Grid.Row="1">
            <ContentControl Content="{Binding NavigationService.CurrentView}" IsTabStop="False"
                            FocusVisualStyle="{x:Null}"/>
        </ui:ScrollViewerEx>
    </Grid>
</Window>